// Code generated by ent, DO NOT EDIT.

package ent

import (
	"assistant-api/internal/ent/action"
	"assistant-api/internal/ent/actionroute"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	pgvector "github.com/pgvector/pgvector-go"
)

// 技能路由文字
type ActionRoute struct {
	config `json:"-"`
	// ID of the ent.
	// 全域唯一主鍵 UUID
	ID uuid.UUID `json:"id,omitempty"`
	// 所屬 action
	ActionID uuid.UUID `json:"action_id,omitempty"`
	// 給 RAG 檢索的文字
	RouteText string `json:"route_text,omitempty"`
	// 向量嵌入（PostgreSQL pgvector）
	Embedding *pgvector.Vector `json:"embedding,omitempty"`
	// 語系，例如 zh-TW, en, ja
	Locale string `json:"locale,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ActionRouteQuery when eager-loading is set.
	Edges        ActionRouteEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ActionRouteEdges holds the relations/edges for other nodes in the graph.
type ActionRouteEdges struct {
	// Action holds the value of the action edge.
	Action *Action `json:"action,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// ActionOrErr returns the Action value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ActionRouteEdges) ActionOrErr() (*Action, error) {
	if e.Action != nil {
		return e.Action, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: action.Label}
	}
	return nil, &NotLoadedError{edge: "action"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ActionRoute) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case actionroute.FieldEmbedding:
			values[i] = &sql.NullScanner{S: new(pgvector.Vector)}
		case actionroute.FieldRouteText, actionroute.FieldLocale:
			values[i] = new(sql.NullString)
		case actionroute.FieldID, actionroute.FieldActionID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ActionRoute fields.
func (_m *ActionRoute) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case actionroute.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case actionroute.FieldActionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field action_id", values[i])
			} else if value != nil {
				_m.ActionID = *value
			}
		case actionroute.FieldRouteText:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field route_text", values[i])
			} else if value.Valid {
				_m.RouteText = value.String
			}
		case actionroute.FieldEmbedding:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field embedding", values[i])
			} else if value.Valid {
				_m.Embedding = new(pgvector.Vector)
				*_m.Embedding = *value.S.(*pgvector.Vector)
			}
		case actionroute.FieldLocale:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field locale", values[i])
			} else if value.Valid {
				_m.Locale = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ActionRoute.
// This includes values selected through modifiers, order, etc.
func (_m *ActionRoute) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAction queries the "action" edge of the ActionRoute entity.
func (_m *ActionRoute) QueryAction() *ActionQuery {
	return NewActionRouteClient(_m.config).QueryAction(_m)
}

// Update returns a builder for updating this ActionRoute.
// Note that you need to call ActionRoute.Unwrap() before calling this method if this ActionRoute
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ActionRoute) Update() *ActionRouteUpdateOne {
	return NewActionRouteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ActionRoute entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ActionRoute) Unwrap() *ActionRoute {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ActionRoute is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ActionRoute) String() string {
	var builder strings.Builder
	builder.WriteString("ActionRoute(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("action_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ActionID))
	builder.WriteString(", ")
	builder.WriteString("route_text=")
	builder.WriteString(_m.RouteText)
	builder.WriteString(", ")
	if v := _m.Embedding; v != nil {
		builder.WriteString("embedding=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("locale=")
	builder.WriteString(_m.Locale)
	builder.WriteByte(')')
	return builder.String()
}

// ActionRoutes is a parsable slice of ActionRoute.
type ActionRoutes []*ActionRoute
